import React from "react";

const PageNotFound = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Sorry, Page not found!
    </div>
  );
};

export default PageNotFound;
